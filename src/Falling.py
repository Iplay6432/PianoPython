import pygame

class Falling(pygame.Rect):
    def __init__ (self, note, screen): #note is the note name
        super.__init__(args)
        self.note = self.note
        self.screen = screen

    def place_key(self):
        pygame.draw.rect(self.screen, , )